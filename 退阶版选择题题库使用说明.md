# 退阶版选择题题库使用说明

## 📚 概述

本选择题题库是基于原有的 `intermediate_questions.js` 填空题题库自动生成的退阶版本，将填空题转换为选择题形式，每个题目提供两个选项：正确答案和一个错误答案。

## ✨ 特点

- **难度降低**：从填空题变为选择题，降低答题难度
- **选项随机**：选项顺序随机排列，避免固定模式
- **智能干扰**：根据答案类型生成合理的错误选项
- **保持分类**：完全保留原有的题目分类和解析
- **快速生成**：一键转换，无需手动修改

## 📊 题库统计

- **总题目数**：1813 题
- **分类数量**：65 个分类
- **主要分类**：
  - 综合练习：15 题
  - 介词综合：28 题
  - 代词综合：70 题
  - 连词综合：多个子分类，共 200+ 题
  - 时态语态：200+ 题
  - 从句综合：100+ 题
  - 名词、形容词、副词等：200+ 题

## 🚀 快速开始

### 1. 引入题库

```javascript
// 在页面的 .js 文件中
const choiceQuestions = require('../../data/choice_questions.js');
```

### 2. 基本使用

```javascript
Page({
  data: {
    currentQuestion: null,
    options: [],
    selectedAnswer: '',
    showResult: false
  },

  onLoad() {
    this.loadRandomQuestion();
  },

  // 加载随机题目
  loadRandomQuestion() {
    const categories = Object.keys(choiceQuestions);
    const randomCategory = categories[Math.floor(Math.random() * categories.length)];
    const questions = choiceQuestions[randomCategory];
    const question = questions[Math.floor(Math.random() * questions.length)];
    
    this.setData({
      currentQuestion: question,
      options: question.options,
      selectedAnswer: '',
      showResult: false
    });
  },

  // 选择答案
  selectAnswer(e) {
    const selected = e.currentTarget.dataset.answer;
    this.setData({
      selectedAnswer: selected,
      showResult: true
    });
  }
});
```

### 3. 页面模板示例

```xml
<!-- 在页面的 .wxml 文件中 -->
<view class="question-container">
  <view class="question-text">{{currentQuestion.text}}</view>
  
  <view class="options-container">
    <view 
      wx:for="{{options}}" 
      wx:key="index"
      class="option-item {{selectedAnswer === item ? 'selected' : ''}}"
      data-answer="{{item}}"
      bindtap="selectAnswer"
    >
      {{item}}
    </view>
  </view>
  
  <view wx:if="{{showResult}}" class="result-container">
    <view class="result-text">
      {{selectedAnswer === currentQuestion.correctAnswer ? '✅ 正确' : '❌ 错误'}}
    </view>
    <view class="correct-answer">
      正确答案：{{currentQuestion.correctAnswer}}
    </view>
    <view class="analysis">{{currentQuestion.analysis}}</view>
  </view>
  
  <button class="next-btn" bindtap="loadRandomQuestion">下一题</button>
</view>
```

## 🔧 高级用法

### 1. 按分类获取题目

```javascript
// 获取特定分类的题目
function getQuestionsByCategory(categoryName) {
  if (choiceQuestions[categoryName]) {
    return choiceQuestions[categoryName];
  }
  return [];
}

// 使用示例
const practiceQuestions = getQuestionsByCategory('综合练习');
console.log(`找到 ${practiceQuestions.length} 道题目`);
```

### 2. 随机获取题目

```javascript
// 随机获取题目（可指定分类）
function getRandomQuestion(categoryName = null) {
  if (categoryName && choiceQuestions[categoryName]) {
    const questions = choiceQuestions[categoryName];
    return questions[Math.floor(Math.random() * questions.length)];
  }
  
  // 随机选择一个分类
  const categories = Object.keys(choiceQuestions);
  const randomCategory = categories[Math.floor(Math.random() * categories.length)];
  const questions = choiceQuestions[randomCategory];
  return questions[Math.floor(Math.random() * questions.length)];
}

// 使用示例
const randomQuestion = getRandomQuestion('介词综合');
```

### 3. 答案检查

```javascript
// 检查答案是否正确
function checkAnswer(question, selectedAnswer) {
  return {
    isCorrect: selectedAnswer === question.correctAnswer,
    correctAnswer: question.correctAnswer,
    analysis: question.analysis
  };
}

// 使用示例
const result = checkAnswer(currentQuestion, userAnswer);
if (result.isCorrect) {
  console.log('回答正确！');
} else {
  console.log(`正确答案是：${result.correctAnswer}`);
}
```

### 4. 获取统计信息

```javascript
// 获取题库统计信息
function getQuestionStats() {
  const stats = {};
  let total = 0;
  
  for (const category in choiceQuestions) {
    const count = choiceQuestions[category].length;
    stats[category] = count;
    total += count;
  }
  
  stats.total = total;
  return stats;
}

// 使用示例
const stats = getQuestionStats();
console.log(`总题目数：${stats.total}`);
console.log(`分类数量：${Object.keys(stats).length - 1}`);
```

## 📁 文件结构

```
miniprogram/
├── data/
│   ├── intermediate_questions.js    # 原始填空题题库
│   └── choice_questions.js         # 新生成的选择题题库
├── generate_choice_questions.js     # 转换脚本
└── choice_questions_example.js     # 使用示例
```

## 🔄 重新生成题库

如果需要重新生成题库（比如原题库有更新），可以运行：

```bash
node generate_choice_questions.js
```

脚本会自动：
1. 读取原始题库文件
2. 解析所有填空题
3. 为每个题目生成错误选项
4. 随机排列选项顺序
5. 保存到新的选择题题库文件

## 💡 使用建议

1. **循序渐进**：可以先从简单的分类开始，如"综合练习"
2. **分类练习**：按语法点分类练习，更有针对性
3. **随机练习**：使用随机题目功能，增加练习的趣味性
4. **错题复习**：记录错题，重点复习
5. **解析学习**：充分利用题目的详细解析

## 🎯 适用场景

- **初学者**：降低答题难度，建立信心
- **复习巩固**：快速复习语法知识点
- **课堂练习**：教师可以快速出题
- **自主学习**：学生可以自主练习
- **考试准备**：为正式考试做准备

## 🔍 题目格式示例

```javascript
{
  "text": "We should be kind ___ animals",
  "options": ["about", "to"],
  "correctAnswer": "to",
  "analysis": "\"be kind to\"为固定短语,意为\"对……友好；善待……\",整句翻译为\"我们应该善待动物\"。",
  "category": "综合练习"
}
```

## 📞 技术支持

如果在使用过程中遇到问题，可以：

1. 检查文件路径是否正确
2. 确认题库文件格式是否完整
3. 查看控制台错误信息
4. 重新运行生成脚本

---

**🎉 祝你使用愉快！这个退阶版选择题题库将帮助你更轻松地学习英语语法！**
