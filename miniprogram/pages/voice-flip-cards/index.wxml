<view class="container">
  <!-- 标题区域 -->
  <view class="header">
    <view class="title">语态单词翻转卡片</view>
    <view class="subtitle">点击卡片翻转查看对应语态</view>
  </view>

  <!-- 卡片区域 -->
  <view class="card-container">
    <view class="card {{isFlipped ? 'flipped' : ''}}" bindtap="flipCard" wx:if="{{cards[currentIndex]}}">
      <!-- 正面 -->
      <view class="card-front">
        <view class="card-content">
          <view class="question-text">{{cards[currentIndex].frontTitle}}</view>
        </view>
      </view>
      
      <!-- 背面 -->
      <view class="card-back">
        <view class="card-content">
          <view class="answer-text">{{cards[currentIndex].backContent}}</view>
        </view>
      </view>
    </view>
  </view>

  <!-- 进度指示器 -->
  <view class="progress-indicator">
    <view class="progress-dots">
      <view class="dot {{index === currentIndex ? 'active' : ''}}" 
            wx:for="{{cards}}" 
            wx:key="id" 
            wx:for-index="index"></view>
    </view>
    <view class="progress-text">{{currentIndex + 1}} / {{totalCards}}</view>
  </view>

  <!-- 导航按钮 -->
  <view class="navigation">
    <button class="nav-btn prev-btn" bindtap="prevCard">
      <text class="nav-icon">←</text>
      <text class="nav-text">上一张</text>
    </button>
    
    <button class="nav-btn next-btn" bindtap="nextCard">
      <text class="nav-text">下一张</text>
      <text class="nav-icon">→</text>
    </button>
  </view>

  <!-- 学习小贴士 -->
  <view class="tips">
    <view class="tips-icon">💡</view>
    <view class="tips-content">
      <view class="tips-title">学习小贴士</view>
      <view class="tips-text">• 先看题目，思考对应的语态转换</view>
      <view class="tips-text">• 注意主动语态和被动语态的结构差异</view>
      <view class="tips-text">• 重点掌握be动词+过去分词的被动结构</view>
    </view>
  </view>
</view> 